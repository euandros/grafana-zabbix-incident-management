# grafana_bc_incidente
Este repositório contém o modelo completo (export em JSON / As-Code format) do dashboard 'Gerenciamento de Incidentes' para o Grafana (v12+), integrado diretamente ao Zabbix e renderizado utilizando o plugin Business Charts (antes Volkov Labs - Apache ECharts).
